# tree-sitter-pike grammar (source) — not committed
tree-sitter-pike.wasm

# Untrack the bundled copy in dist (published with npm package)
pz|!dist/tree-sitter-pike.wasm
# Bundler artifacts in dist/
dist/web-tree-sitter.wasm
